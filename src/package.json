{
  "name": "text-reveal",
  "version": "1.0.0",
  "type": "module",
  "description": "Scroll Text Reveal static site",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "test": "vitest run",
    "test:watch": "vitest",
    "preview": "vite preview",
    "predeploy": "npm run build"
  },
  "devDependencies": {
    "gh-pages": "^6.3.0",
    "vite": "^8.0.0"
  }
}